# Branch 20260610 Setup

## Background

The server checkout at `/home/baibh/DBChaos` was already on the latest `20260520`. The user requested a new branch `20260610`, and also requested that the three legacy generated XML files still appearing as untracked under `scripts/` be added to `.gitignore`.

## Actions

- Created branch `20260610` from the latest `20260520` (`bb9fa3e`).
- Added these server-side generated XML paths to `.gitignore`:
  - `scripts/opengauss_tpcc_config_chaosblade.xml`
  - `scripts/tpcc_worker.xml`
  - `scripts/fault-cases-generic.xml`
- Updated `.agents/index.md` current working branch to `20260610`.

## Notes

- This change was made only in the server repository at `/home/baibh/DBChaos`.
