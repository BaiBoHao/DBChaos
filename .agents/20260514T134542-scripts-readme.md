# Scripts README

## Background

The user requested that the current branch remain `20260514`, that the latest remote branch be pulled first, and that a clear README be added under `scripts/` documenting how to use `generate_chaosblade_configs.py`, including commands and parameters.

## Actions

- Confirmed current branch was `20260514`.
- Pulled `origin/20260514` with fast-forward update to commit `902fba3`.
- Added `scripts/README.md`.
- Documented:
  - output files generated by the script
  - quick-start commands
  - partial selection and interactive selection
  - common parameters
  - supported DBChaos fault keys
  - JSON selection file format

## Validation

- Ran `python scripts/generate_chaosblade_configs.py --list`.
- The command listed all 16 configured DBChaos fault keys successfully.

## Notes

- Existing uncommitted changes to `DBChaos-0.0.1.jar`, `resources/chaos.properties`, `resources/db.properties`, and `utils/convert_cases.py` were not touched.
