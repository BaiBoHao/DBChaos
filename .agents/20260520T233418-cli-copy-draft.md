# CLI Copy Draft

## Background

After the root README draft was approved, the next step was to raise the quality of the terminal help output so it presents DBChaos more like a polished open source tool and less like a classroom demo.

## Changes

- Rewrote `resources/chaos.properties` copy to align with the new positioning:
  - DBChaos as an adversity injection tool built around database kernel subsystems
  - resilience boundary and resilience capability probing
- Reworked `src/chaos/Main.java` help output:
  - usage line now uses the actual jar name `DBChaos-0.0.1.jar`
  - added a kernel subsystem section
  - replaced the old flat fault-profile wording with entry-keyword wording
  - cleaned up error messages, global options, and examples
- Kept the actual injection routing and runtime behavior unchanged.

## Validation

- `javac --release 8 -encoding UTF-8 -cp lib/* -d target/codex-cli-check src/**/*.java`
- `java -cp target/codex-cli-check;resources;lib/* chaos.Main --help`

## Notes

- This is still an uncommitted draft change.
- Existing local changes outside this task were left untouched.
