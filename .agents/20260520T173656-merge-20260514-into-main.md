# Merge 20260514 Into Main

## Background

The user explicitly requested merging the latest `20260514` branch work into `main`. On the server repository at `/home/baibh/DBChaos`, `20260514` and `origin/main` did not share a merge base, so this was not a normal fast-forward or ordinary merge.

## Actions

- Created local branch `main` to track `origin/main`.
- Started a merge from `20260514` into `main` with `--allow-unrelated-histories`.
- Preserved the actual work produced on `20260514`:
  - `.agents/` records
  - `scripts/README.md`
  - `scripts/generate_configs.py`
  - `.gitignore`
  - `src/chaos/Main.java`
  - `src/chaos/inject/DuplicateTxnInject.java`
  - `src/chaos/inject/MaxPreparedInject.java`
- Kept `main`'s root `README.md` and `lib/` deletion state, because those were not part of the intended `20260514` feature work and would otherwise be pulled in only due to unrelated-history merge behavior.

## Validation

- `python3 -m py_compile scripts/generate_configs.py` passed.
- `javac --release 8 -encoding UTF-8 src/chaos/Main.java src/chaos/core/BaseFaultInject.java src/chaos/inject/*.java` passed with only JDK 8 deprecation warnings.

## Notes

- This merge was completed directly on the server repository at `/home/baibh/DBChaos`.
- The target branch after merge is `main`.
