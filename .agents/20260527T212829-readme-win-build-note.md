# README Windows Build Note

## Background

The user requested a minimal root README adjustment so the project clearly explains how to build under a local Windows environment using the build script already present in the project root.

## Changes

- Added a short Windows build note to the root `README.md`.
- Kept the original Linux/macOS `build.sh` instructions.
- Added PowerShell usage for:
  - `.\build_for_win.ps1 build`
  - `.\build_for_win.ps1 preview-help`

## Notes

- This is a minimal documentation-only adjustment.
- No commit has been created yet.
