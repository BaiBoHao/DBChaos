package chaos.inject;

import chaos.core.BaseFaultInject;

/**
 * {{CASE_TITLE}} 故障注入实现。
 * 通过模板生成后，请补充实际注入逻辑、参数校验与清理策略。
 */
public class {{CLASS_NAME}} extends BaseFaultInject {
    private static final String RESET  = "\u001B[0m";
    private static final String CYAN   = "\u001B[36m";
    private static final String YELLOW = "\u001B[33m";
    private static final String RED    = "\u001B[31m";
    private static final String BOLD   = "\u001B[1m";

    public {{CLASS_NAME}}(String dbType) {
        super(dbType, "{{CASE_KEY_UPPER}}");
    }

    @Override
    public void execute(String[] args) throws Exception {
        if (args.length == 0 || hasArg(args, "-h") || hasArg(args, "--help")) {
            printHelp();
            return;
        }

        String durationStr = getArg(args, "-duration");
        if (durationStr == null) {
            System.err.println(RED + " 参数错误: 缺失必填参数 -duration (ms)" + RESET);
            printHelp();
            return;
        }

        long durationMs = Long.parseLong(durationStr);

        System.out.println(CYAN + " >>> " + RESET + BOLD + "启动 {{CASE_TITLE}}" + RESET);
        System.out.println("   持续时间: " + durationMs + " ms");

        // TODO: 在这里补充具体注入逻辑。
    }

    @Override
    public void printHelp() {
        System.out.println("\n" + BOLD + "不利 Case 用法: " + YELLOW + "{{SUBSYSTEM}} {{CASE_KEY}}" + RESET);
        System.out.println("  {{CASE_DESCRIPTION}}");
        System.out.println("\n" + BOLD + "参数列表:" + RESET);
        System.out.printf("  %-15s %s\n", "-duration", "必填。故障总时长 (ms)");
        System.out.println("\n" + BOLD + "示例:" + RESET);
        System.out.println(CYAN + "  java -jar DBChaos-0.0.1.jar --db opengauss {{SUBSYSTEM}} {{CASE_KEY}} {{EXAMPLE_ARGS}}" + RESET);
    }
}
